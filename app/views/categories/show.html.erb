<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<ul class="list-group">

  <% @category.posts.url_not_nil.each do |post|%>
    <li class="list-group-item"> <%= post.title %> </li>
  <% end %>
</ul>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
